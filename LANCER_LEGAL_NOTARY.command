#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
cd "$DIR/legal-notary-suite"
echo "Lancement de Legal Notary (Office Notarial de Côte d'Ivoire)..."
open "index.html"
